server:
  port: 9094

spring:
  profile:
    active: dev
  application:
    name: ORDER-SERVICE
  data:
    mongodb:
      uri: mongodb://localhost:27017/orderdb
      host: localhost
      port: 27071
      database: orderdb
      repositories:
        enabled: true
